name: main

on:
  push:
    branches:
      - main
    tags:
      - '*'

  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13', '3.14', 'pypy3.10', 'pypy3.11']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up uv with Python ${{ matrix.python }}
      uses: astral-sh/setup-uv@v8.2.0
      with:
        python-version: ${{ matrix.python }}

    - name: Run tests for ${{ matrix.python }}
      run: |
        case "${{ matrix.python }}" in
          '3.10') ENVS="py310-pytest{70,71,72,73,74,80,81,82,83,84,90,91}" ;;
          'pypy3.10') ENVS="pypy310-pytest{70,71,72,73,74,80,81,82,83,84,90,91}" ;;
          '3.11') ENVS="py311-pytest{73,74,80,81,82,83,84,90,91}" ;;
          'pypy3.11') ENVS="pypy311-pytest{73,74,80,81,82,83,84,90,91}" ;;
          '3.12') ENVS="py312-pytest{74,80,81,82,83,84,90,91}" ;;
          '3.13') ENVS="py313-pytest{84,90,91}" ;;
          '3.14') ENVS="py314-pytest{84,90,91}" ;;
          *) echo "Unsupported python: ${{ matrix.python }}" ; exit 1 ;;
        esac
        uvx --from 'tox>=4.55,<5' tox run -x "tox.envlist=${ENVS}"

    - name: Lint everything with Ruff and mypy
      if: matrix.python == '3.14'
      run: uvx --from 'tox>=4.55,<5' tox run -e lint

    - name: Ensure full coverage
      if: matrix.python == '3.14'
      run: uvx --from 'tox>=4.55,<5' tox run -e coverage

  publish:
    if: |
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/') &&
      github.repository == 'pytest-dev/pytest-describe'
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: '3.14'

      - name: Build package
        run: uv build

      - name: Publish package
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          attestations: true
